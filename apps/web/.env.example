# apps/web/.env.example — copy to apps/web/.env
# In dev, requests are proxied to the API (see vite.config.ts), so relative
# paths work out of the box. Override for a separately-hosted API in prod.
VITE_API_URL=
VITE_WS_URL=
